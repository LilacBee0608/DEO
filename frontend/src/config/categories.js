// ============================================================
// 全站分类/标签统一配置(唯一数据源)
// 使用方:
//   1) Home.vue      首页分类标签栏(筛选)
//   2) Upload.vue    投稿页标签选择列表
// 同步机制:
//   只需修改本文件中的 categories 数组(增/删/改),
//   首页标签栏与投稿页可选标签会自动同步添加/删除,
//   无需改动任何页面组件。
// 注: "全部"(value='')是首页筛选专用,不属于投稿可选标签,
//      Home.vue 在使用时自行拼接。
// ============================================================

/** 可投稿标签列表(label 为显示名,value 为存储值) */
export const categories = [
  { label: '编程', value: '编程' },
  { label: '美食', value: '美食' },
  { label: '动漫', value: '动漫' },
  { label: '游戏', value: '游戏' },
  { label: '音乐', value: '音乐' }
]

/** 仅提取标签值(字符串数组),供需要纯标签列表的场景使用 */
export const categoryValues = categories.map(c => c.value)
