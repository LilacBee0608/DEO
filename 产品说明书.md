# DEO 视频弹幕互动网站 — 产品说明书

> 版本：v1.0.0 · 更新日期：2026-08-25

---

## 一、系统简介

### 1.1 项目定位

**DEO（Danmu-Enabled Online video platform，弹幕互动视频网站）** 是一套面向教学/实训场景的、仿 B 站风格的轻量级视频分享平台。它以「前后端分离 + 单体后端」的主流工业形态搭建，完整覆盖用户从「浏览 → 登录 → 投稿 → 观看（弹幕/评论/点赞/收藏）→ 个人中心（观看历史/收藏夹）」的全链路交互，是一份可直接作为课程设计、团队实训、全栈开发教学演示的完整项目。

核心特点：

- **全栈闭环**：从 SQL 建表 → 后端 ORM/鉴权/业务 → 前端路由/状态/组件/接口请求，整条链路均可运行、可调试、可扩展；
- **弹幕原生体验**：内置覆盖在 `<video>` 上的 Canvas 弹幕层，支持实时发送、滚动弹幕、自定义颜色、时间轴对齐；
- **分 P 投稿与播放**：单视频支持多分 P（剧集/分段），前端选集列表与推荐列表分层展示；
- **个人数据中心**：观看历史自动去重（同视频重复观看只更新时间）、收藏夹联合主键去重、一键清空历史；
- **协作友好**：后端锁定字节码兼容 JDK 17（LTS），注释详尽，已配套一键脚本与 IDEA 修复脚本，降低团队成员上手门槛。

### 1.2 架构形态（基于代码证据）

```
┌─────────────────────────────────────────────────────────────────────┐
│                              浏览器客户端                              │
│   Vue 3 + Router + Pinia + Element Plus + 自制 DanmuPlayer(Canvas)    │
│   Dev 端口: 5173  |  打包: Vite 5    |  静态资源路径 @ -> src          │
└──────────────────────────────────────┬───────────────────────────────┘
                                       │ axios (baseURL=/api)
                                       │ Vite 开发代理: /api → :8081
                                       ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     Spring Boot 3.5 单体后端 API                       │
│   端口: 8081  |  Context-Path: /api  |  JWT Bearer 鉴权                │
│   ┌────────┬─────────┬─────────┬──────────┬──────────┬──────────┐    │
│   │ Auth   │ Videos  │ Danmu   │ Comments │  User    │ 静态资源 │    │
│   │ 登录注册│ 视频CRUD│ 弹幕CRUD │ 评论CRUD  │ 历史/收藏 │ /files/* │    │
│   └────┬───┴────┬────┴────┬────┴────┬─────┴────┬─────┴────┬─────┘    │
│        │        │         │         │          │          │          │
│   MyBatis-Plus 3.5.12  +  JSqlParser  +  MySQL 8.0 连接池              │
│        ▼        ▼         ▼         ▼          ▼          ▼          │
│   v_user   video     danmu   v_comment  history   本地磁盘存储:        │
│   v_like   v_favorite              (评论点赞)  videos / covers / logo  │
└─────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
                     MySQL 8.0 (数据库名: deo, 共 8 张表)
```

### 1.3 技术栈清单（均与仓库 pom.xml / package.json / application.yml 逐项对应）

| 层级 | 技术 | 版本 | 在项目中的作用 |
|------|------|------|---------------|
| **前端框架** | Vue | 3.4.27 | 响应式视图层 |
| **前端工程** | Vite | 5.2.11 | 开发服务器（5173）+ 打包构建 |
| **前端路由** | Vue Router | 4.3.2 | 7 个页面路由，路由守卫拦截未登录访问 |
| **前端状态** | Pinia | 2.1.7 | 用户登录态持久化（localStorage 持久层） |
| **前端 UI** | Element Plus | 2.7.6 | 导航/表单/卡片/弹窗/头像/Tab 等组件 |
| **前端图标** | @element-plus/icons-vue | 2.3.1 | Upload / Search / VideoPlay 等图标 |
| **前端 HTTP** | Axios | 1.7.2 | 统一请求封装（自动携带 JWT、解包 Result、401 跳转登录） |
| **后端框架** | Spring Boot | 3.5.0 | 启动器 Web / Validation / Test |
| **后端持久层** | MyBatis-Plus | 3.5.12 + JSqlParser | CRUD / 分页拦截器 / Lambda 查询 |
| **数据库驱动** | mysql-connector-j | 由 Boot 父 POM 锁定 | MySQL 8.0 连接 |
| **后端鉴权** | JJWT | 0.12.5 | 登录签发 Bearer Token，拦截器校验 |
| **后端工具** | Hutool | 5.8.27 | 加密、IO、日期等通用工具 |
| **后端代码生成** | Lombok | 1.18.46 | @Data / @Slf4j / RequiredArgsConstructor |
| **字节码目标** | Java | --release 24（同时兼容 JDK 17 运行时） | 编译时显式指定目标字节码 |
| **文件存储** | 本地磁盘 | `${user.dir}/videos` `/covers` `/logo` | 视频、封面、Logo 本地落盘 |
| **静态资源 URL 前缀** | Spring MVC 映射 | `/files/*` | 前端以 `/api/files/*` 形式通过代理访问 |

---

## 二、产品介绍

### 2.1 页面与路由（与 router/index.js 一一对应）

| 路径 | 页面组件 | 权限 | 功能说明 |
|------|---------|------|---------|
| `/` | Home.vue | 游客 | 首页视频卡片瀑布流，显示封面、标题、UP主名、播放量、点赞数；顶部搜索框按标题模糊匹配；分页 |
| `/video/:vId` | VideoDetail.vue | 游客可浏览，登录可互动 | **核心播放页**：① DanmuPlayer：原生 `<video>` + Canvas 覆盖弹幕层，支持按帧弹幕、发送弹幕、颜色自定义；② 视频信息条：UP主名、播放量、点赞按钮（回显当前用户点赞态）、收藏按钮（回显当前用户收藏态，刷新不丢失）、分享；③ 简介；④ 评论区：列表（昵称+内容+点赞数）、发送、评论点赞；⑤ 右侧两栏：多 P 时优先显示「选集」，否则直接显示「推荐视频」（推荐池取自首页全量随机，排除当前视频、不重复，数量随容器高度自适应）；⑥ 进入页面自增播放量 1 次；⑦ 登录用户自动写入观看历史（后端去重）|
| `/login` | Login.vue | 游客 | 用户名 + 密码登录，登录成功写入 Pinia userStore + localStorage（token + userInfo），未登录访问需授权路由时自动 redirect 回来 |
| `/register` | Register.vue | 游客 | 用户名 + 密码注册，密码由后端 BCrypt 加密入库（Hutool SecureUtil） |
| `/upload` | Upload.vue | 需登录 | 视频投稿：标题、标签、封面上传、**单/分 P 投稿**（单 P 一个视频 URL，分 P 通过「添加多个视频」按钮追加多行，最终以换行符拼接写入 Video.videoUrl） |
| `/user` | UserCenter.vue | 需登录 | **个人主页**：① 顶部用户信息卡片（头像 + 昵称 + 观看记录数/收藏数）；② 双 Tab：「观看历史」— 按时间倒序的视频卡片网格，显示上次观看时间，支持「清空历史」（二次确认弹窗）；「收藏夹」— 按播放量倒序展示已收藏视频卡片；③ 任意卡片点击跳回对应视频详情 |
| `/:pathMatch(.*)*` | NotFound.vue | — | 404 兜底页 |

### 2.2 后端接口模块（5 个 Controller，20 个接口）

#### 2.2.1 认证 AuthController

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/auth/register` | 游客 | 注册。用户名校验唯一，密码 BCrypt 加密。返回用户 id |
| POST | `/auth/login` | 游客 | 登录。校验密码 → 签发 JWT（24 小时）。返回 `{ token, user: {...} }` |
| GET  | `/auth/me` | 未登录返回 401 | 从 Token 解析当前用户信息（密码脱敏） |

#### 2.2.2 视频 VideoController

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET    | `/videos/list` | 游客 | 分页查询。参数：`page / size / title / tags`。返回 records + total + page + size |
| GET    | `/videos/detail/{vId}` | 游客可看，登录用户回查点赞/收藏态 | 详情 VO：视频基本信息 + authorName + liked（当前用户是否点赞）+ favorited（是否收藏）|
| POST   | `/videos` | 需登录 | 投稿。生成 vId（`v` + UUID 前 8 位），把当前登录用户 id 作为作者 |
| PUT    | `/videos/{vId}` | 需登录 + 作者本人 | 修改视频信息 |
| DELETE | `/videos/{vId}` | 需登录 + 作者本人 | 删除视频，级联清理 v_like / v_favorite |
| POST   | `/videos/play/{vId}` | 游客/登录 | 播放量 +1（`UPDATE SET play_num = play_num + 1`）|
| POST   | `/videos/like/{vId}` | 需登录 | 点赞切换。返回 `{ liked: true/false }` |
| POST   | `/videos/favorite/{vId}` | 需登录 | 收藏切换。返回 `{ favorited: true/false }` |

#### 2.2.3 弹幕 DanmuController

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET  | `/danmu/list/{vId}` | 游客 | 查询该视频全部弹幕（did / id / vId / danmuContent / danmuFrame / color）|
| POST | `/danmu` | 需登录 | 发送弹幕。danmuFrame 为弹幕出现秒数位置，颜色默认 `#FFFFFF` |

#### 2.2.4 评论 VCommentController

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET  | `/comments/list/{vId}` | 游客 | 查询视频全部评论（含发布者昵称，未找到昵称时显示「匿名」）|
| POST | `/comments` | 需登录 | 发布评论 |
| POST | `/comments/like/{cid}` | 需登录 | 评论点赞（可扩展为切换，当前为递增）|

#### 2.2.5 用户 UserController（个人中心）

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST   | `/user/history/{vId}` | 需登录 | 记录观看。`INSERT ... ON DUPLICATE KEY UPDATE watch_time = NOW()`，同用户同视频自动去重只更新时间 |
| GET    | `/user/history` | 需登录 | 观看历史列表（联表 video + v_user，含 authorName / cover_url / watch_time），按时间倒序 |
| DELETE | `/user/history` | 需登录 | 清空当前用户全部观看历史 |
| GET    | `/user/favorites` | 需登录 | 收藏夹列表（联表 video + v_user），按播放量倒序 |

### 2.3 数据库与数据实体（8 张表，8 个 Entity）

| 表名 | 主键 | 唯一约束/联合主键 | 业务含义 | 对应 Entity |
|------|------|-------------------|---------|------------|
| v_user | id INT AUTO_INCREMENT | UNIQUE(user_name) | 用户表（昵称、BCrypt 密码、注册时间） | VUser |
| video | v_id VARCHAR(32) | PRIMARY(v_id) | 视频表（标题、标签、封面、视频URL 支持多行分 P、描述、作者 id、播放/点赞/分享数、创建时间）| Video |
| v_comment | cid INT AUTO_INCREMENT | — | 视频评论（用户id、视频v_id、评论内容、创建时间）| VComment |
| danmu | did INT AUTO_INCREMENT | — | 弹幕（用户id、视频v_id、弹幕内容、出现帧/秒、颜色）| Danmu |
| v_like | — | **联合主键 (id, v_id)** | 视频点赞（同用户同视频只 1 条）| VLike |
| v_favorite | — | **联合主键 (id, v_id)** | 视频收藏（同用户同视频只 1 条，天然去重）| VFavorite |
| history | hid INT AUTO_INCREMENT | **UNIQUE(user_id, v_id)** | 观看历史（同用户同视频只 1 条，重复观看 update watch_time）| History |

> **关于 authorName 字段的说明**：Video / History / VFavorite 列表 SQL 都会 `LEFT JOIN v_user ON video.id = CAST(v_user.id AS CHAR)` 取 `user_name`，并以 **SQL 别名 `authorName`**（camelCase）返回，确保 MyBatis 在非数据库字段上直接映射为前端可用驼峰字段，不依赖下划线转驼峰的隐式行为。

### 2.4 核心业务机制（从代码抽取的重要规则）

1. **去重写入（收藏 / 点赞 / 观看历史）**
   - 点赞、收藏依靠联合主键天然防重，后端 toggle 逻辑：若已存在则删除，不存在则插入，返回 `{liked: true}`；
   - 观看历史依靠 `UNIQUE(user_id, v_id)` + `INSERT ... ON DUPLICATE KEY UPDATE watch_time = NOW()`，重复观看只刷新时间不新增行。

2. **登录态与游客态的双层鉴权**
   - JwtInterceptor 内部分为两类路径：`GUEST_ALLOWED_PATHS`（`/videos/list`、`/videos/detail/**`、`/danmu/list/**`、`/comments/list/**`）和其余「必须登录」路径；
   - 游客路径下：无 Token → 放行不注入 userId；有 Token 且有效 → 注入 userId（使得 detail 接口对登录用户返回 liked / favorited = true，这是刷新后按钮不恢复默认的关键）；Token 过期但路径游客可访问 → 静默放行；
   - 必须登录路径下：无 Token 或 Token 过期 → 401；有效 → 注入 userId。

3. **分 P 投稿与播放**
   - 投稿时前端多行输入最终以换行符 `\n` 拼接写入 `video.videoUrl`；
   - VideoDetail 进入后按 `\n` 拆分 `partList`，长度 > 1 时在右侧显示「选集」Tab，选中分 P 时通过 `:key="currentPartIndex"` 强制 DanmuPlayer 重新挂载切换视频源。

4. **Jackson 字段 vId 的历史兼容性处理**
   - 所有包含 `vId` 的 Entity / DTO（Video、VComment、Danmu、VLike、VFavorite、CommentSendDTO、DanmuSendDTO）均采用「**字段 + 显式 getter/setter 都加 `@JsonProperty("vId")`**」的写法；
   - 不再依赖 `onMethod_` 语法（该语法依赖特定 Lombok 版本易导致编译失败）；
   - 这样 Jackson 不会把 getVId() 误推断为 `vid`，前后端字段名完全一致，避免 `video/undefined` 等路由错误。

5. **静态资源访问**
   - 视频、封面、网站 Logo 本地落盘在 `${user.dir}/videos /covers /logo`；
   - WebMvcConfig 中注册 3 个 ResourceHandler，对外 URL 前缀为 `/files/*`；
   - 前端通过 Vite 代理 `(/api → :8081)` 以 `/api/files/videos/xxx.mp4` 访问，避免跨域与本地路径暴露。

---

## 三、环境搭建

### 3.1 软硬件要求

#### 3.1.1 操作系统

Windows 10 / 11（64-bit，推荐 19042 及以上）。macOS / Linux 可运行但本文档部分脚本需适配 bash。

#### 3.1.2 必装软件（含最低版本）

| 软件 | 最低版本 | 建议版本 | 用途 | 下载 |
|------|---------|---------|------|------|
| JDK | 17 (LTS) | 17.0.12 或 24.0.1 | 后端编译与运行。Lombok 1.18.46 同时兼容 17-24 | <https://adoptium.net/temurin/releases/?version=17> |
| Node.js | 18.x | 18.20 LTS 或 20.15 LTS | 前端 Vite、npm 包管理。Vite 5 要求 Node ≥ 18 | <https://nodejs.org/> |
| MySQL | 8.0 | 8.0.37 或 8.4 LTS | 数据库服务，用户名 root、密码按本机配置 | <https://dev.mysql.com/downloads/installer/> |
| Git | 任意 | 2.45+ | 克隆仓库、协作 | <https://git-scm.com/download/win> |
| IntelliJ IDEA | 2023.2 | 2024.1 及以上（Community 即可） | 后端 Java 开发，**必装 Lombok 插件**（IDEA 已内置，首次打开项目如果报红请在 Settings → Build → Compiler → Annotation Processors 勾选 Enable annotation processing）| <https://www.jetbrains.com/idea/download/> |
| Visual Studio Code | 1.80+ | 最新稳定版 | 前端 Vue 开发，**必装 Volar（Vue Language Features）** + ESLint + Prettier + 中文语言包 | <https://code.visualstudio.com/> |

#### 3.1.3 可选 VSCode 插件（团队协作建议统一）

可通过 VSCode 命令行一键安装（任选其一执行）：

```powershell
code --install-extension Vue.volar
code --install-extension dbaeumer.vscode-eslint
code --install-extension esbenp.prettier-vscode
code --install-extension MS-CEINTL.vscode-language-pack-zh-hans
code --install-extension ritwickdey.liveserver
```

> 若 `code` 命令不可用，请在 VSCode 中按 Ctrl+Shift+P → 输入 `Install 'code' command in PATH` 安装。

### 3.2 项目初始化（从零到可运行，5 步）

#### Step 1：克隆代码

```bash
git clone https://github.com/<你的仓库>/DEO.git
cd DEO
```

目录结构（顶层）：

```
DEO/
├─ backend/              # Spring Boot 后端(Maven 工程, pom.xml 在此)
│  └─ src/main/java/com/bili/demo/
│     ├─ controller/     # 5 个 Controller
│     ├─ service/ + impl/
│     ├─ mapper/         # MyBatis-Plus BaseMapper + 自定义 SQL
│     ├─ entity/         # 8 个数据表实体
│     ├─ dto/ + vo/      # 请求参数 DTO 与返回 VO
│     ├─ interceptor/    # JwtInterceptor
│     ├─ config/         # WebMvcConfig（静态资源、拦截器注册）
│     └─ utils/          # JwtUtils 等
├─ frontend/             # Vue 3 前端(Vite 工程, package.json 在此)
│  └─ src/
│     ├─ views/          # 7 个页面组件
│     ├─ components/     # Navbar / DanmuPlayer 等共用组件
│     ├─ router/         # vue-router 配置 + 路由守卫
│     ├─ stores/         # Pinia userStore(登录态持久化)
│     ├─ api/            # axios 封装 + video/danmu/comment/user/auth 各模块
│     └─ styles/
├─ videos/   covers/   logo/   # 运行时自动创建: 存储上传的视频/封面/网站Logo
├─ sql/                  # (可选) 建表 SQL 脚本(建议创建并随仓库保存)
└─ .gitignore            # 忽略 .idea/ .vscode/ node_modules/ target/ out/
```

#### Step 2：创建 MySQL 数据库与 8 张表

在 MySQL 客户端（Navicat / DBeaver / mysql CLI）执行：

```sql
CREATE DATABASE IF NOT EXISTS deo DEFAULT CHARACTER SET utf8mb4;
USE deo;

-- 1. 用户表
CREATE TABLE IF NOT EXISTS v_user (
  id          INT AUTO_INCREMENT PRIMARY KEY,
  user_name   VARCHAR(64) NOT NULL UNIQUE,
  user_pswd   VARCHAR(128) NOT NULL,
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 2. 视频表 (作者 id 字段保留原字段名 id VARCHAR,与 v_user.id 做转换)
CREATE TABLE IF NOT EXISTS video (
  v_id        VARCHAR(32) PRIMARY KEY,
  id          VARCHAR(32),
  title       VARCHAR(255) NOT NULL,
  tags        VARCHAR(255),
  cover_url   VARCHAR(500),
  video_url   TEXT NOT NULL COMMENT '分P时以换行符分隔',
  description TEXT,
  play_num    INT DEFAULT 0,
  like_num    INT DEFAULT 0,
  share_num   INT DEFAULT 0,
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 3. 评论表
CREATE TABLE IF NOT EXISTS v_comment (
  cid             INT AUTO_INCREMENT PRIMARY KEY,
  id              VARCHAR(32) COMMENT '发布用户id(保留原字段名)',
  v_id            VARCHAR(32) NOT NULL,
  comment_content VARCHAR(500) NOT NULL,
  like_num        INT DEFAULT 0,
  create_time     DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 4. 弹幕表
CREATE TABLE IF NOT EXISTS danmu (
  did          INT AUTO_INCREMENT PRIMARY KEY,
  id           VARCHAR(32) COMMENT '发布用户id',
  v_id         VARCHAR(32) NOT NULL,
  danmu_content VARCHAR(200) NOT NULL,
  danmu_frame   INT NOT NULL COMMENT '弹幕出现秒数',
  color        VARCHAR(16) DEFAULT '#FFFFFF',
  create_time  DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 5. 视频点赞 (联合主键去重)
CREATE TABLE IF NOT EXISTS v_like (
  id   INT NOT NULL,
  v_id VARCHAR(32) NOT NULL,
  PRIMARY KEY (id, v_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 6. 视频收藏 (联合主键去重)
CREATE TABLE IF NOT EXISTS v_favorite (
  id   INT NOT NULL,
  v_id VARCHAR(32) NOT NULL,
  PRIMARY KEY (id, v_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 7. 观看历史 (UNIQUE(user_id, v_id) 去重)
CREATE TABLE IF NOT EXISTS history (
  hid        INT AUTO_INCREMENT PRIMARY KEY,
  user_id    INT NOT NULL,
  v_id       VARCHAR(32) NOT NULL,
  watch_time DATETIME NOT NULL,
  UNIQUE KEY uk_user_video (user_id, v_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

#### Step 3：修改后端数据库账号密码（重要！）

打开 `backend/src/main/resources/application.yml`，把 `datasource.username` 与 `datasource.password` 改成你本机 MySQL 的账号密码：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/deo?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root       # ← 改成你的 MySQL 账号
    password: 000922     # ← 改成你的 MySQL 密码
```

> 可选优化：在本机新建 `application-local.yml`，用 `--spring.profiles.active=local` 覆盖敏感配置，避免 `application.yml` 提交到仓库后密码泄露。

#### Step 4：安装前端依赖

```bash
cd frontend
npm install
# 国内网络慢,可先切淘宝镜像(一次性):
#   npm config set registry https://registry.npmmirror.com
```

首次运行会下载 Vue / Vite / Element Plus / Pinia / Axios 等到 `node_modules/`（150-300MB，约 5-10 分钟）。后续无需再跑 `npm install`，除非 `package.json` 有改动。

#### Step 5：编译 & 下载后端依赖

在 IDEA 中操作（推荐）：

1. 用 IDEA 打开 **`backend/pom.xml`** → 弹窗提示 **Open as Project**（**不要直接打开根目录 DEO**，否则 IDEA 会把项目根误当成 Java Source Root，出现「程序包不存在」编译错误）；
2. IDEA 右侧 Maven 面板 → 点击「刷新」按钮（Reload All Maven Projects），等待 MyBatis-Plus / JJWT / Hutool / Lombok 全部下载（首次 5-15 分钟）；
3. **Settings → Build → Compiler → Annotation Processors** → 勾选 **Enable annotation processing**（Lombok 必须开启）；
4. **Build → Rebuild Project** （强制全量重编译，避免 IDEA JPS 缓存导致的 `TypeTag::UNKNOWN` 等报错）。

### 3.3 启动项目（3 种方式任选）

#### 方式 A：IDEA 单步启动 + 浏览器手动打开（新手最稳）

1. **启动后端**：在 IDEA 中定位到 `backend/src/main/java/com/bili/demo/BiliDemoApplication.java`，右键 → Run 'BiliDemoApplication'。
   - 控制台看到 `Started BiliDemoApplication in ... seconds` 说明成功，后端已监听 `http://localhost:8081/api`。
   - 若开启了 `frontend.auto-start: true`，Spring 启动成功后会自动弹出一个新 cmd 窗口执行 `npm run dev`（此时可直接跳到第 3 步）。

2. **启动前端**（若 auto-start 关闭）：打开终端
   ```bash
   cd frontend
   npm run dev
   ```
   Vite 启动会自动打开浏览器 `http://localhost:5173/`。

3. 访问前端地址，点击注册 → 登录 → 浏览视频 → 投稿 → 观看 → 收藏 → 回到个人中心查看历史/收藏夹，即可验证功能完整。

#### 方式 B：IDEA 仅启动后端，依赖 auto-start 自动拉起前端

- 确保 `application.yml` 中 `frontend.auto-start: true`，`frontend.dir` 留空（自动探测）；
- 直接 Run BiliDemoApplication；
- 后端启动到末尾时，会打开一个标题为「DEO前端-Vite」的新窗口运行 Vite。

> 提示：若 Vite 窗口一闪而过没启动，通常是 `node_modules/` 未安装。请按 3.2 Step 4 执行 `npm install`。

#### 方式 C：生产部署（基础版）

```bash
# 1) 前端打包(输出到 frontend/dist)
cd frontend
npm run build

# 2) 后端打包(输出 backend/target/bili-demo-1.0.0.jar)
cd backend
mvn clean package -DskipTests

# 3) 运行(示例: 使用外部数据库,生产时请把 JWT secret换掉)
java -jar -Djwt.secret=<生产环境强口令> -Dspring.datasource.password=<DB密码> \
     target/bili-demo-1.0.0.jar
```

然后把 `frontend/dist/*` 丢到 Nginx 站点目录，Nginx 同时配置 `/api` 反向代理到 `http://127.0.0.1:8081/api`、`/files/*` 反向代理到 `http://127.0.0.1:8081/api/files/*`，即可完成前后端合一部署。详细 Nginx 配置超出本说明书范围，可按需扩展。

### 3.4 端口汇总与跨域说明

| 服务 | 地址 | 备注 |
|------|------|------|
| 前端 Vite 开发服务器 | http://localhost:5173/ | Vite 启动时 autoOpen 自动打开 |
| 后端 Spring Boot API | http://localhost:8081/api | Context-Path 为 `/api` |
| 后端静态资源 URL | http://localhost:8081/api/files/* | 通过 Vite 代理访问形式为 `http://localhost:5173/api/files/*` |
| MySQL | localhost:3306 / deo | application.yml 可改端口与库名 |

**为什么不存在跨域问题？**

- 开发态：Vite 的 `vite.config.js` 中配置了 `proxy: { '/api': 'http://localhost:8081' }`，浏览器只向 `:5173` 发请求，Vite 内部转发到 `:8081/api`，同源。
- 生产态：打包的静态 HTML 与后端 API 同域提供（或 Nginx 反向代理统一域名）。

### 3.5 常见搭建问题速查（来自项目开发实际踩坑，非经验臆测）

| 现象 | 根因 | 修复方法 |
|------|------|---------|
| IDEA 编译报 `程序包 com.xxx 不存在` | IDEA 把项目根当成 Java Source Root（打开项目时点错了目录）| 删除 `.idea/` → `out/` → `backend/target/`，然后 IDEA 中 **File → Open → 选择 `backend/pom.xml` → Open as Project** |
| 编译报 `找不到符号 onMethod_()` | 组员本地 Lombok 缓存了旧版本，1.18.20 之前没有 onMethod_ 语法 | 项目已通过「显式 getter/setter + @JsonProperty」绕过该语法，刷新 Maven → Rebuild Project 即可；若仍报错请强制更新依赖：在 backend 目录下执行 `mvn clean install -U` |
| 编译报 `TypeTag::UNKNOWN` / `ExceptionInInitializerError` | 本机 javac 版本（比如 JDK24）与 Lombok 版本不匹配 | 已锁定 Lombok 1.18.46（兼容 JDK17-24）。刷新 Maven + Rebuild Project。仍不行请确认 IDEA 的 Project SDK（File → Project Structure → SDK）|
| 启动报 `Table 'deo.history' doesn't exist` 等表不存在 | 没执行 3.2 Step 2 的建表 SQL | 在 MySQL 里逐行执行建表脚本即可，无需重启后端 |
| 登录后刷新 → 详情页「收藏按钮」回到未激活 | 旧版本：放行路径 `/videos/detail/**` 被 exclude，导致 JWT 即使带 token 也不会被解析，detail 的 liked/favorited 恒 false | 已修复（JwtInterceptor 内部 GUEST_ALLOWED_PATHS + 软解析）。拉取最新代码 + 重启后端 |
| 收藏后立刻切换页面正常，但刷新就失效（同上面一行）| 同上 | 同上 |
| 点击视频跳转 URL 变成 `/video/undefined` | Jackson 把 `vId` 自动序列化成 `vid`，前端取不到 | 已修复（显式 getter/setter + @JsonProperty("vId") 在 7 个类上）|
| 前端启动 `[plugin:vite:import-analysis] Failed to resolve import "/api/files/logo/logo.png"` | 把后端静态资源路径写进 `<img src="/api/files/logo/logo.png">`，Vite 编译时当成本地模块 import | 已修复：`Navbar / Login / Register` 均使用 `const logoUrl = '/api/files/logo/logo.png'` + `:src="logoUrl"` 动态绑定，避免 Vite 静态解析 |
| 页面 `favicon` 不显示 | 浏览器缓存，或 Logo 文件不存在 | 把网站 logo 以 `logo.png` 放入 `${user.dir}/logo/logo.png`；强制刷新 Ctrl+F5 |
| 上传投稿时文件大小超限 | Spring 配置了 500MB 默认上限，单视频超过 500MB | 改 application.yml 的 `spring.servlet.multipart.max-file-size / max-request-size` |
| 历史记录里作者名全显示「匿名」| 之前 SQL 别名用 snake_case（`author_name`），MyBatis 对非数据库字段不做下划线转驼峰 | 已修复（SQL 别名统一 `authorName`，与 Java VO/Map 字段一致）|

### 3.6 团队协作快速起步（给新组员）

1. 克隆仓库 → 按 3.1 安装环境；
2. 执行 3.2 Step 2（建表 SQL）；
3. 按 3.2 Step 3 修改数据库密码；
4. `cd frontend` → `npm install`；
5. IDEA Open → 选 `backend/pom.xml` → Open as Project → 等待 Maven 依赖下载完 → 右上角 Rebuild；
6. 右键 Run BiliDemoApplication 即可。
